[package]
name = "xtask"
version.workspace = true
edition.workspace = true
description = "Build orchestration for Sentinella eBPF programs"

[dependencies]
anyhow.workspace = true
clap = { version = "4", features = ["derive"] }
